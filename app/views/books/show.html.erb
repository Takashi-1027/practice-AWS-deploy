<div class='container'>
  <div class='row'>
    <div class='col-md-3'>
      <!--<h2>User info</h2>-->
      <!--[20. NameError undefined local variable or method 'user']の修正を以下のように修正した-->
      <!--修正箇所< %= render 'users/info' (,users: @book.user)<==これを追加した。！ %>-->
      <%= render 'users/info', user: @book.user %>　<!--users/infoはusersファイルの中の_info.html.erb(部分テンプレートを呼び出す)を-->
      <h2 class="mt-3">New book</h2>
      <%= render 'form', book: @book_new %> <!-- formはbooksファイルの中の_form.html.erb(部分テンプレートを呼び出す)-->
    </div>
    <div class='col-md-8 offset-md-1'>

      <!--bookの詳細を表示する為のレンダー------------------------------------------------------------------------------------------>
      <div id="book_detaile">
        <%= render 'book_detaile_info', book: @book %>
      </div>

      <!--コメント一覧するためのレンダー------------------------------------------------------------------------------------------->
      <div id="comments_list" class="comments my-5">
        <%= render 'book_comments/book_comments_list', book: @book %>
      </div>

      <!--エラーメッセージ用のレンダー------------------------------------------------------------------------>
      <div class="new-comment">

        <!--コメントフォームのレンダー------------------------------------------------------------------------>
        <div id="comments_section">
          <%= render 'book_comments/book_comments_section', {book: @book, book_comment: @book_comment} %>
        </div>
      </div>
    </div>
  </div>
</div>